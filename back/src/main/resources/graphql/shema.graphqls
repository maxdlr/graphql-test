type Query {
  userById(id: ID): User
  teamById(id: ID): Team
  users: [User]
  teams: [Team]
}

type Team {
  id: ID,
  name: String,
  createdAt: String
}

type User {
  id: ID,
  username: String,
  team: Team,
  createdAt: String
}
