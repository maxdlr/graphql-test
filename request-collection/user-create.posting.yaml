name: user create
method: POST
url: http://localhost:8080/graphql
body:
  content: |-
    {
      "query": "mutation CreateUser { CreateUser(username: Polygone)}"
    }
  content_type: application/json
headers:
- name: content-type
  value: application/json
