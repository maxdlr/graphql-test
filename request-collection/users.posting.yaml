name: users
description: Get all users
method: POST
url: http://localhost:8080/graphql
body:
  content: |-
    {
      "query": "query users { users { id username team {id name createdAt} createdAt }}"
    }
  content_type: application/json
headers:
- name: content-type
  value: application/json
