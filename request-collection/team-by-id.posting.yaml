name: team by id
description: Get all users
method: POST
url: http://localhost:8080/graphql
body:
  content: |-
    {
      "query": "query teamById { teamById(id: 1) { id name createdAt }}"
    }
  content_type: application/json
headers:
- name: content-type
  value: application/json
