name: teams
description: Get all teams
method: POST
url: http://localhost:8080/graphql
body:
  content: |-
    {
      "query": "query GetAllTeams { GetAllTeams { id name createdAt } }"
    }
  content_type: application/json
headers:
- name: content-type
  value: application/json
